import { CalculateBirthDate } from './calculate-birth-date';

describe('CalculateBirthDate', () => {
  it('should create an instance', () => {
    expect(new CalculateBirthDate()).toBeTruthy();
  });
});
